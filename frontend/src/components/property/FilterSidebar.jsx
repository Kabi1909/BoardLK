import {SlidersHorizontal,X} from 'lucide-react';
import {districts,cities,propertyTypes,roomTypes,genders,facilities} from '../../data/locations';
import {FormInput,FormSelect,Checkbox} from '../forms/Fields';
export function SortDropdown({value,onChange}){return <select className="sort-select" aria-label="Sort properties" value={value||'latest'} onChange={e=>onChange(e.target.value)}><option value="latest">Latest listings</option><option value="price-asc">Price: low to high</option><option value="price-desc">Price: high to low</option><option value="rating">Highest rated</option><option value="popular">Most popular</option></select>;}
export default function FilterSidebar({filters,onChange,onReset,onClose}){const set=(k,v)=>onChange({...filters,[k]:v,...(k==='district'?{city:''}:{})});return <aside className="filter-sidebar"><div className="section-heading"><h3><SlidersHorizontal size={16}/> Filters</h3>{onClose?<button className="icon-button" onClick={onClose} aria-label="Close filters"><X size={16}/></button>:<button className="text-link" onClick={onReset}>Reset</button>}</div><label className="filter-label">Monthly budget</label><div className="grid-2"><FormInput label="Minimum (Rs.)" type="number" min="0" value={filters.min||''} onChange={e=>set('min',e.target.value)}/><FormInput label="Maximum (Rs.)" type="number" min="0" value={filters.max||''} onChange={e=>set('max',e.target.value)}/></div><FormSelect label="District" placeholder="All districts" options={districts} value={filters.district||''} onChange={e=>set('district',e.target.value)}/><FormSelect label="City" placeholder="All cities" options={filters.district?cities[filters.district]:Object.values(cities).flat().sort()} value={filters.city||''} onChange={e=>set('city',e.target.value)}/><FormSelect label="Property type" placeholder="All property types" options={propertyTypes} value={filters.type||''} onChange={e=>set('type',e.target.value)}/><FormSelect label="Room type" placeholder="All room types" options={roomTypes} value={filters.roomType||''} onChange={e=>set('roomType',e.target.value)}/><FormSelect label="Gender preference" placeholder="All preferences" options={genders} value={filters.gender||''} onChange={e=>set('gender',e.target.value)}/><div className="divider"/><label className="filter-label">Facilities</label>{facilities.map(f=><Checkbox key={f} label={f} checked={(filters.facilities||'').split(',').includes(f)} onChange={e=>{const current=(filters.facilities||'').split(',').filter(Boolean);set('facilities',(e.target.checked?[...current,f]:current.filter(x=>x!==f)).join(','));}}/>)}<div className="divider"/><Checkbox label="Available now" checked={!!filters.available} onChange={e=>set('available',e.target.checked?'true':'')}/><FormSelect label="Minimum rating" placeholder="Any rating" options={['3','4','4.5','5']} value={filters.rating||''} onChange={e=>set('rating',e.target.value)}/>{onClose&&<button className="btn" onClick={onClose}>Show results</button>}</aside>;}
