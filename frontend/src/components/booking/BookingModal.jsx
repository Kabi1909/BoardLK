import {useState} from 'react';
import {Modal} from '../common/UI';
import {FormInput,FormSelect,FormTextarea} from '../forms/Fields';
import {useAuth} from '../../context/AuthContext';
import {requestBooking} from '../../services/actions';
import {today} from '../../utils/format';
export default function BookingModal({property,onClose}){const {user}=useAuth();const [f,setF]=useState({moveIn:'',occupants:'1',duration:'6 months',customDuration:'',message:''});const [errors,setErrors]=useState({});const [error,setError]=useState('');const [success,setSuccess]=useState(false);const set=(k,v)=>setF({...f,[k]:v});function submit(e){e.preventDefault();const errors={};if(!f.moveIn||f.moveIn<today())errors.moveIn='Choose today or a future date.';if(!Number.isInteger(Number(f.occupants))||f.occupants<1||f.occupants>property.spaces)errors.occupants='Choose between 1 and '+property.spaces+' occupants.';if(!f.duration)errors.duration='Choose a stay duration.';if(f.duration==='Custom'&&!f.customDuration.trim())errors.customDuration='Enter your expected stay duration.';if(f.message.trim().length<10)errors.message='Write at least 10 characters for the owner.';setErrors(errors);if(Object.keys(errors).length)return;try{requestBooking(user,property,{...f,duration:f.duration==='Custom'?f.customDuration:f.duration});setSuccess(true);}catch(e){setError(e.message);}}return <Modal title={success?'Request sent!':'Request a booking'} onClose={onClose}>{success?<><div className="success">Your request is pending. The owner will review it and respond in your bookings.</div><button className="btn full-width" onClick={onClose}>Done</button></>:<form onSubmit={submit} noValidate><p className="booking-property">{property.title}</p><div className="grid-2"><FormInput label="Move-in date" type="date" min={today()} required value={f.moveIn} error={errors.moveIn} onChange={e=>set('moveIn',e.target.value)}/><FormInput label="Number of occupants" type="number" min="1" max={property.spaces} required value={f.occupants} error={errors.occupants} onChange={e=>set('occupants',e.target.value)}/></div><FormSelect label="Expected stay duration" options={['1 month','3 months','6 months','1 year','Long term','Custom']} required value={f.duration} error={errors.duration} onChange={e=>set('duration',e.target.value)}/>{f.duration==='Custom'&&<FormInput label="Custom duration" value={f.customDuration} error={errors.customDuration} onChange={e=>set('customDuration',e.target.value)}/>}<FormTextarea label="Message to owner" placeholder="Introduce yourself and share what you’re looking for…" required value={f.message} error={errors.message} onChange={e=>set('message',e.target.value)}/>{error&&<p role="alert" className="error-box">{error}</p>}<p className="fine-print">A booking request is an expression of interest. Confirm visit arrangements and terms directly with the owner.</p><div className="actions"><button type="button" className="btn secondary" onClick={onClose}>Cancel</button><button className="btn">Send request</button></div></form>}</Modal>;}
